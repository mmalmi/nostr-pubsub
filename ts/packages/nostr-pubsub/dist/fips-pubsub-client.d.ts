import { type NostrEvent, type NostrFilter, type NostrVerifiedEvent } from './types.js';
import { FipsPubsubWireCodec } from './wire.js';
import { type FipsNostrPubsubClientLimits, type FipsNostrPubsubClientOptions, type FipsNostrPubsubEventHandler, type FipsNostrPubsubSubscription } from './fips-pubsub-client-types.js';
export declare const FIPS_NOSTR_PUBSUB_CAPABILITY = "nostr.pubsub/1";
export declare class FipsNostrPubsubClient {
    readonly limits: FipsNostrPubsubClientLimits;
    readonly codec: FipsPubsubWireCodec;
    private readonly node;
    private readonly peers;
    private readonly allowedKinds?;
    private readonly onError;
    private peerSubscriptions;
    private readonly subscriptions;
    private readonly cachedEvents;
    private readonly cachedEventOrder;
    private readonly pending;
    private unregisterService?;
    private removePeerListener?;
    private removeSessionListener?;
    private nextSubscriptionId;
    constructor(options: FipsNostrPubsubClientOptions);
    start(): this;
    stop(): Promise<void>;
    subscribe(filters: NostrFilter[], handler: FipsNostrPubsubEventHandler): FipsNostrPubsubSubscription;
    publish(event: NostrEvent | NostrVerifiedEvent): Promise<void>;
    refreshPeers(): void;
    peerSubscriptionCount(peerId: string): number;
    activeSubscriptionCount(): number;
    idle(): Promise<void>;
    private handle;
    private forwardEvent;
    private normalizedFilters;
    private refreshSubscriptionPeers;
    private sendSubscriptionRequest;
    private closeSubscription;
    private deliver;
    private replayLocal;
    private replayEvents;
    private rememberEvent;
    private admitEvent;
    private currentPeers;
    private send;
    private background;
    private track;
    private handlePeerEvent;
    private handleSessionEvent;
    private dropPeer;
    private requireStarted;
    private report;
}
//# sourceMappingURL=fips-pubsub-client.d.ts.map